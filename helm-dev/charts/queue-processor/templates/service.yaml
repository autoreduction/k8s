apiVersion: v1
kind: Service
metadata:
  name: {{ include "queue-processor.fullname" . }}
  labels:
    {{- include "queue-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "queue-processor.selectorLabels" . | nindent 4 }}
