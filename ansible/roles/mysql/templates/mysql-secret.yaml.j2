apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: "mysql-secret"
  namespace: "webapp"
data:
  DATABASE_ROOT_PASSWORD: "{{ db_secrets.root_password | b64encode }}"
  DATABASE_NAME: YXV0b3JlZHVjdGlvbg==
  DATABASE_USERNAME: "{{ db_secrets.username | b64encode }}"
  DATABASE_USER_PASSWORD: "{{ db_secrets.user_password | b64encode }}"
  DATABASE_HOST: "{{ db_secrets.host | b64encode }}"
